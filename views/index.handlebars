<h1>Eat-Da-Burger</h1>
<img src ="https://www.kindpng.com/picc/m/5-52036_hamburger-picture-transparent-transparent-background-burger-clipart-hd.png" alt="burger_img" width="200px" height="200px"></img>
<div class = "main">
  <div class="notEaten">
    <h2>Burgers to be Eaten</h2>
      {{#each burgers}}
          {{#unless devoured}}
              <p>{{id}}. {{burger_name}} <button data-id="{{id}}" class="devour_now"> Devour</button></p>
              
          {{/unless}}
      {{/each}}
  </div>

  <div class="devoured">
    <h2>Burgers Devoured</h2>

      {{#each burgers}}
          {{#if devoured}}
              <p>{{id}}. {{burger_name}}</p>
          {{/if}}
      {{/each}}

  </div>
</div>
<div class ="add">
  <h2>Add a Burger</h2>
  <form class="create-form">
    <div class="form-group">
      
      <input type="text" placeholder="Burger Name" id="burger" name="newBurger"><button type="submit">Add Burger</button>
    </div>
    
</div>
  
</form>